// SPDX-License-Identifier: GPL-3.0-or-later

int main(void) {
  // RuntimeHost will broker ZinRT and later managed runtimes. It is deliberately
  // inert until user-mode processes and IPC transport exist.
  return 0;
}
